# RSVP-app
RSVP app for an event

## How to run this app

At the moment, this repository only contains this README and does not yet include runnable application code (for example, no `package.json`, server, or frontend files).

Once the app source code is added, run instructions can be documented here (install dependencies, start command, etc.).
